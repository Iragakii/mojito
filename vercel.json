{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/videos/:path*",
      "destination": "/videos/:path*"
    }
  ]
}
